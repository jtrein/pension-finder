import type { PensionData } from "../../types/pensions";
import { PENSION_POTS_FIXTURE } from "./pensions";
import { SEARCHED_PENSIONS_FIXTURE } from "./searched";

export const ALL_POTS_FIXTURE: PensionData = {
  pensionPots: PENSION_POTS_FIXTURE,
  searchedPensions: SEARCHED_PENSIONS_FIXTURE,
};
